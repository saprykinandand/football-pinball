export const PHYSICS_TUNING = {
  runnerFps: 120,
  maxSubsteps: 3,
  positionIterations: 10,
  velocityIterations: 8,
  constraintIterations: 4,
  maxBallSpeed: 15,
  ballRadius: 12,
  ballDensity: 0.0022,
  ballFriction: 0.012,
  ballFrictionAir: 0.004,
  ballRestitution: 0.78,
  wallRestitution: 0.55,
  bumperRestitution: 0.92,
  bumperImpulse: 0.009,
  flipperKick: 0.012,
  wallPadding: 12,
  goalPadding: 10,
  playerPadding: 4,
  bumperPadding: 4,
  flipperPadding: 0,
  launchImpulse: 0.012,
  launchForceMin: 0.012,
  launchForceMax: 0.02,
  launchDelayMs: 300,
  launchXMin: -0.35,
  launchXMax: 0.35,
  launchXDeadZone: 0.03,
}

export function createDefaultParams() {
  return {
    gravityY: 1.05,
    maxBallSpeed: PHYSICS_TUNING.maxBallSpeed,
    ballRestitution: PHYSICS_TUNING.ballRestitution,
    bumperRestitution: PHYSICS_TUNING.bumperRestitution,
    wallRestitution: PHYSICS_TUNING.wallRestitution,
    bumperImpulse: PHYSICS_TUNING.bumperImpulse,
    flipperSpeed: 0.18,
    flipperKick: PHYSICS_TUNING.flipperKick,
    goalkeeperSpeed: 78,
    wallPadding: PHYSICS_TUNING.wallPadding,
    goalPadding: PHYSICS_TUNING.goalPadding,
    playerPadding: PHYSICS_TUNING.playerPadding,
    bumperPadding: PHYSICS_TUNING.bumperPadding,
    flipperPadding: PHYSICS_TUNING.flipperPadding,
    showVisualShapes: true,
    showMatterBodies: true,
    showSafetyBodies: true,
    showAlignmentCompare: false,
    freezePhysics: false,
    launchForceMin: PHYSICS_TUNING.launchForceMin,
    launchForceMax: PHYSICS_TUNING.launchForceMax,
    launchDelayMs: PHYSICS_TUNING.launchDelayMs,
    launchXMin: PHYSICS_TUNING.launchXMin,
    launchXMax: PHYSICS_TUNING.launchXMax,
    launchXDeadZone: PHYSICS_TUNING.launchXDeadZone,
  }
}
